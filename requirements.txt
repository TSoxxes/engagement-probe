# Core analysis dependencies. These alone are enough to run the test suite and
# every src/analyze*.py entry point.
numpy>=1.26,<3
pandas>=2.1,<3
matplotlib>=3.8,<4

# Generation only (src/generate.py). Kaggle and Colab images already ship a
# GPU build of torch, so installing this file there is a no-op for torch.
# Installing it elsewhere gets the default CPU/CUDA wheel for your platform;
# see https://pytorch.org/get-started/locally/ if you need a specific build.
torch>=2.2
transformers>=4.45,<5
accelerate>=0.34,<2
huggingface-hub>=0.25,<2
sentencepiece>=0.2,<1
